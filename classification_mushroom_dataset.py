# -*- coding: utf-8 -*-
"""Classification - Mushroom Dataset.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1EMXAfNHa3HTSS2s3_7yfa-Gb9wEuqu_Y
"""

import pandas as pd
df = pd.read_csv('mushroom_dataset_new.csv')
df.head()

df.head(44)

df ['mushroom'].value_counts()

from sklearn.preprocessing import LabelEncoder
df = df.apply(LabelEncoder().fit_transform)
df

df ['mushroom'].value_counts()

from sklearn.model_selection import train_test_split

features = df.iloc[:,:21]
label = df.iloc[:,21] 

x_train, x_test, y_train, y_test = train_test_split(features, label, test_size = 0.20, random_state = 42)
x_test

from sklearn.naive_bayes import GaussianNB
model = GaussianNB()
model = model.fit(x_train, y_train)

#from sklearn.tree import DecisionTreeClassifier
#model = DecisionTreeClassifier()
#model = model.fit(x_train, y_train)

#from sklearn.neighbors import KNeighborsClassifier
#model = KNeighborsClassifier(n_neighbors)
#model = model.fit(x_train, y_train)

print(model.predict(x_test))
print(y_test)

from sklearn.metrics import accuracy_score
hasil_prediksi = model.predict(x_test)
accuracy_score(y_test, hasil_prediksi)

from sklearn.metrics import confusion_matrix
confusion_matrix(hasil_prediksi, y_test)

from sklearn.metrics import classification_report
print(classification_report( y_test, hasil_prediksi))